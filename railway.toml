[build]
buildCommand = "pip install -r requirements.txt && .venv/bin/python manage.py collectstatic --noinput"

[deploy]
startCommand = "./.venv/bin/python -m gunicorn Muslim_AI.wsgi:application --bind 0.0.0.0:${PORT:-8000}"
